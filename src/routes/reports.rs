// Report read/write/download routes
//
// /api/reports                          — list reports
// /api/reports/:folder/:file            — get / update
// /api/reports/:folder/:file/download   — download raw .md
// /api/reports/:folder/zip              — download folder as .zip
// /api/reports/:folder/attachments/:file — download attachment
