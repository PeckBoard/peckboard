// Misc routes — config, usage, keep-awake, models, workflows
//
// /api/config      — get / update defaults
// /api/models      — model registry
// /api/workflows   — built-in workflows
// /api/keep-awake  — sleep-blocker toggle
// /api/coop/status — status line
