// Provider registry — holds all available providers and their models
//
// Built-in Claude provider always registered.
// Plugin providers register via provider.register hook.
// Model IDs are provider:model format (e.g. claude:opus, openai:gpt-4o).
