# 1.1 Project Scaffolding

**Phase:** Foundation
**Depends on:** Nothing

## Tasks

- [ ] PB-001: Set up React + Vite + TypeScript in `web/`
- [ ] PB-002: Configure Vite proxy to Axum backend (dev mode)
- [ ] PB-003: Configure `rust-embed` to serve `web/dist/` (production mode)
- [ ] PB-004: Set up Zustand store with initial slices (auth, ui)
- [ ] PB-005: Set up shared types between frontend and backend (API contracts)
- [ ] PB-006: Verify dev workflow: `cargo run` + `cd web && npm run dev` both work together

## Done When

- Frontend renders a placeholder page at `http://localhost:5173`
- Backend serves the built frontend at `http://localhost:3333`
- Vite proxies `/api` requests to the backend
- Zustand store initializes without errors
