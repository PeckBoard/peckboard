# 11.1 Server Lifecycle

**Phase:** Lifecycle and Hardening
**Depends on:** 6.2

## Tasks

- [ ] PB-409: Implement graceful shutdown (signal handler → drain WS connections → stop workers → close DB)
- [ ] PB-410: Implement startup state repair: detect dangling agent-starts (no closing agent-end) → synthesize agent-end{crashed, reason: peckboard-crash}
- [ ] PB-411: Implement session recovery: resume workers via provider resume for cards with in-flight worker sessions
- [ ] PB-412: Implement folder-missing detection on recovery (append folder-missing event)
- [ ] PB-413: Wire server lifecycle hooks (started, shutdown before/after)
- [ ] PB-414: Wire HTTP route hooks (request before/after/failed with per-plugin endpoint grants from manifest)
- [ ] PB-415: Write integration tests: start → create state → kill → restart → verify dangling agent-ends synthesized
- [ ] PB-416: Write integration tests: start → spawn workers → kill → restart → verify workers resume

## Done When

- Server shuts down gracefully (no orphaned processes, no corrupt state)
- Startup repairs dangling runs and resumes workers
- Missing folders detected and logged
- Server lifecycle and HTTP route hooks fire
