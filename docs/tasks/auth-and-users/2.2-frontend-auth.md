# 2.2 Frontend Auth

**Phase:** Auth and Users
**Depends on:** 2.1

## Tasks

- [ ] PB-064: Build LoginModal component (username, password, remember me checkbox)
- [ ] PB-065: Build RegisterModal component (username, email, password, confirm password — shown when no users exist)
- [ ] PB-066: Implement auth store slice (token storage in localStorage/sessionStorage, authed flag, login/logout/changePassword actions)
- [ ] PB-067: Implement `authedFetch` wrapper (attach JWT, handle 401 → clear token → show LoginModal)
- [ ] PB-068: Implement auth status check on app load (GET /api/auth/status → show RegisterModal or LoginModal)
- [ ] PB-069: Write unit tests: token storage (localStorage vs sessionStorage based on remember-me)
- [ ] PB-070: Write unit tests: 401 handling clears token and flips authed flag
- [ ] PB-071: Write unit tests: auth state transitions (unauthenticated → login → authenticated → logout → unauthenticated)

## Done When

- First visit with no users shows registration modal
- Subsequent visits show login modal
- Token persists across page refreshes (remember me) or dies with tab (session storage)
- 401 from any API call bounces user to login
