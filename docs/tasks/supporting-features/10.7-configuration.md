# 10.7 Configuration

**Phase:** Supporting Features
**Depends on:** 2.1

## Tasks

- [ ] PB-396: Implement config loading precedence (CLI args > env vars > config.json > defaults)
- [ ] PB-397: Implement GET /api/config (defaultProvider, defaultSessionModel, defaultProjectModel, defaultSessionEffort, defaultProjectEffort)
- [ ] PB-398: Implement PUT /api/config (update defaults, validate values)
- [ ] PB-399: Wire config hooks (update — before/after)
- [ ] PB-400: Build settings/options page (default provider, default model, default effort, appearance settings)
- [ ] PB-401: Write unit tests: config precedence, validation

## Done When

- Config loads from all sources with correct precedence
- Settings page allows changing defaults
- Default provider configurable
