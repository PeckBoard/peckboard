# 7.1 Reports Backend

**Phase:** Reports
**Depends on:** 3.2

## Tasks

- [ ] PB-289: Implement report write (markdown to `<dataDir>/reports/<date>/<file>.md` with frontmatter: title, date, sessionId, projectName)
- [ ] PB-290: Implement report name sanitization (no path separators, dots, traversal — collision-avoiding filenames)
- [ ] PB-291: Implement GET /api/reports (list all report metadata)
- [ ] PB-292: Implement GET /api/reports/:folder/:file (read with parsed frontmatter + body)
- [ ] PB-293: Implement PUT /api/reports/:folder/:file (update body, preserve frontmatter, 1 MB cap)
- [ ] PB-294: Implement GET /api/reports/:folder/:file/download (raw .md, Content-Disposition: attachment)
- [ ] PB-295: Implement GET /api/reports/:folder/zip (zip all markdown in folder via in-memory zip builder)
- [ ] PB-296: Implement GET /api/reports/attachments (list all report attachments)
- [ ] PB-297: Implement attachment write (base64 → disk, allowlisted extensions, 10 MB cap, .meta.json sidecar)
- [ ] PB-298: Implement GET /api/reports/:folder/attachments/:file (download with nosniff + attachment disposition)
- [ ] PB-299: Implement POST /api/reports/:folder/:file/discuss (create session with report as attachment)
- [ ] PB-300: Wire report hooks (write/update/delete/attach — before/after/failed)
- [ ] PB-301: Write unit tests: name sanitization, frontmatter round-trip, traversal guards, collision avoidance
- [ ] PB-302: Write integration tests: write → read → update → download → zip → discuss

## Done When

- Reports can be written, listed, read, updated, downloaded, and zipped
- Attachments can be uploaded with extension/size enforcement
- Discuss creates a new session with the report attached
- All report hooks fire
