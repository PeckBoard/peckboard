# 4.2 Session Messaging

**Phase:** Claude CLI Integration
**Depends on:** 4.1

## Tasks

- [ ] PB-165: Implement send message flow: append user event → resolve provider → spawn or resume → stream response events
- [ ] PB-166: Implement cancel (kill provider process, append agent-end{crashed, reason: operator-stop})
- [ ] PB-167: Implement interrupt (soft abort via provider.interrupt, append interrupt event)
- [ ] PB-168: Implement idle process sweeper (configurable interval + threshold, skip workers, skip mid-turn)
- [ ] PB-169: Implement idle sweeper post-wake skip (first sweep after wake detection is skipped)
- [ ] PB-170: Wire idle sweeper hooks (sweep.before, kill.before/after)
- [ ] PB-171: Write integration tests: send → receive streaming response → send again (resume)
- [ ] PB-172: Write integration tests: send → cancel → resume
- [ ] PB-173: Write integration tests: idle sweep kills process → next send respawns via resume

## Done When

- Users can send messages and receive streaming responses via any registered provider
- Cancel kills the provider process, interrupt aborts the current generation
- Idle processes are swept after configurable timeout
- Swept sessions resume transparently on next message
